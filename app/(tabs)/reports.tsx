import { Text, View } from "react-native";

export default function ReportsScreen() {
	return (
		<View className="flex items-center justify-center h-full">
			<Text>Tela de Comparação de preços!</Text>
		</View>
	);
}
